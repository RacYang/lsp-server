# 测试

## 层次

1. 针对确定性包的单元测试。
2. 针对麻将示例的夹具测试。
3. 针对房间编排的服务测试。
4. 针对单进程 MVP 的端到端测试。

## 覆盖率策略

覆盖率阈值在 `.build/config.yaml` 中配置。生成代码与非关键编排路径按策略排除。

## 负例样本

每条约束 enforcer 须有隔离的负例，存放在 `.build/negatives`。
