# 集群

## 演进

项目从单进程可执行文件起步，后续拆分为多个服务，而无需重写房间语义。

## 节点

- `gate`：套接字归属与推送扇出。
- `lobby`：登录、建房、匹配与元数据。
- `room`：牌桌执行与结算。

## 路由

房间亲和性基于房间 ID。`gate` 解析房间节点，并在迁移前保持对该房间的请求粘性。

## 安全缩容

1. 将节点标记为不可调度。
2. 停止分配新房间。
3. 排空活跃房间。
4. 从服务发现注销。
