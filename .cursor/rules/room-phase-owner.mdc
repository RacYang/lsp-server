---
kind: constraint
description: 房间 RoundState 阶段切换须经 enterPhase 入口
globs: "internal/service/room/**/*.go"
adr: docs/adr/0045-phase-deadline-single-owner-and-phase-token.md
enforcer: scripts/verify-room-phase-owner.py
negative_test: .build/negatives/room_phase_owner_direct_write.go.neg
---

# 房间阶段所有权（ADR-0045）

`RoundState.phaseReason` 与 `RoundState.phaseStartUnixMs` 是 deadline 派生的唯一权威来源。

- 仅允许在 `internal/service/room/phase.go::enterPhase` 内直接赋值；
- 持久化恢复路径（`engine_persist.go::buildRoundStateFromPersist` /
  `finalizeRoundInvariants`）属于"重新加载快照"语义，可视作 enterPhase 的等价初始化；
- engine、scheduler、actor 与其它任何分支都必须改为 `rs.enterPhase(reason)`；
- 校验由 `make verify-room-phase-owner` 执行；
- 违反时 verify 立即失败，提示开发者按 ADR-0045 改写。
