---
kind: constraint
description: Rule 必须声明 ADR、执行器与负例三元组
globs: ".cursor/rules/*.mdc"
adr: docs/adr/0042-agent-governance.md
enforcer: scripts/verify-source-shape.py
negative_test: .build/negatives/rule_shape_missing_enforcer.mdc.neg
---

# Rule 形态

- 每条 rule 必须是 `kind: constraint`。
- 每条 rule 必须声明 `adr`、`enforcer` 与 `negative_test`。
