---
kind: constraint
description: 错误处理必须保留可追溯错误链
globs: "**/*.go"
adr: docs/adr/0000-engineering-charter.md
enforcer: .golangci.yml#{errorlint}
negative_test: .build/negatives/error_handling_missing_wrap.go.neg
---

# 错误处理

- 包装错误时使用 `%w` 保留错误链。
- 不用字符串拼接或 `%v` 吞掉上游错误语义。
