id: rule
purpose: 新增 Cursor rule 骨架
applies_to_glob: ".cursor/rules/*.mdc"
coverage_cell: evolve/rule
verify:
  - kind: regex
    path: "{target}"
    pattern: "kind: constraint[\\s\\S]+adr:[\\s\\S]+enforcer:[\\s\\S]+negative_test:"
related_rules: [rule-shape]
related_skills: [add-constraint]
