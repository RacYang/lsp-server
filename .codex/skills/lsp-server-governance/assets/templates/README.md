# Agent 模板索引

本目录保存代码骨架、注释样板、文件头与文档骨架。新增模板请使用 `/add-template` 命令，并为每个模板目录提供 `manifest.yaml`。

## 分类

- `code/`：Go 包、handler、service、repository、actor、CLI、proto 与表驱动测试骨架。
- `comment/`：导出函数等注释样板。
- `header/`：生成物、Shell、Python 等文件头样板。
- `doc/`：ADR、rule、command、runbook 与 template 文档骨架。
