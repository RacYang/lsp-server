# GENERATED from .build/config.yaml - DO NOT EDIT
version: "2"
linters:
  default: none
  enable:
    - govet
    - errcheck
    - staticcheck
    - ineffassign
    - unused
    - gosec
    - revive
    - gocritic
    - errorlint
    - bodyclose
    - noctx
    - testifylint
  settings:
    revive:
      rules:
        - name: exported
          disabled: true
        - name: package-comments
          disabled: true
        - name: unused-parameter
          disabled: true
  exclusions:
    paths:
      - ".*/gen/.*"
formatters:
  enable:
    - gofmt
    - goimports
  exclusions:
    paths:
      - ".*/gen/.*"
run:
  timeout: 5m
