func Test{{Subject}}(t *testing.T) {
	tests := []struct {
		name string
		// TODO: 补充输入字段。
		wantErr bool
	}{
		{name: "正常路径"},
	}

	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			// TODO: 执行被测行为并断言完整结果。
		})
	}
}
