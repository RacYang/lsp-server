# 本地开发默认配置（Phase 1 单体）
server:
  addr: ":18080"
rule:
  default_id: "sichuan_xzdd"
cluster:
  lobby_addr: ""
  room_addr: ""
redis:
  addr: ""
postgres:
  dsn: ""
obs:
  addr: ""
etcd:
  endpoints: ""
runtime:
  gate:
    ws_rate_limit_per_second: 20
    ws_rate_limit_burst: 40
    ws_idempotency_cache: 4096
  room:
    mailbox_capacity: 96
  redis:
    idempotency_ttl: 10m
  logging:
    level: info
    format: json
    otel_enabled: false
    otel_endpoint: ""
    dynamic_level: false
    sample:
      initial: 0
      thereafter: 0
      tick: 1s
      error_never: true
